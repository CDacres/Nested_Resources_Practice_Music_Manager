<p>Title: <%= song.title %> - <em><%= song.duration %></em>
	<%= link_to "Show", album_song_path(@album, song) unless controller_name == "songs" %>
	<%= link_to "Edit", edit_album_song_path(@album, song) if controller_name == "songs" %>
	<%= link_to "Destroy", album_song_path(@album, song), method: :delete, data: { confirm: "Are you sure?" } %>
</p>